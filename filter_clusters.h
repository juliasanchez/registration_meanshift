#ifndef FILTER_CLUSTERS
#define FILTER_CLUSTERS

#include <iostream>
#include <string>
#include <vector>
#include "size_cluster.h"

void filter_clusters(std::vector<Cluster>& clusters);

#include "filter_clusters.inl"

#endif // FILTER_CLUSTERS
